---
# En-tête ajouté a posteriori (J4-CHAINE, 25/09/2026) : le rapport d'origine n'en avait pas.
lot: J1-L2
titre: "Robustesse du site"
date: 2026-09-11
statut: termine
redacteur: agent-J1-L2
controleur: aucun
changesets: []
fiches_creees: []
fiches_modifiees: []
lacunes: []
decisions_humaines: []
---
# Rapport de lot J1-L2 — Robustesse du site

- **Agent** : agent d'exécution, développeur Astro
- **Date** : 2026-09-11
- **Statut** : terminé (build Astro non vérifiable localement : à confirmer par la CI)

## Ce qui a été fait
- **`scripts/rename_slug.py`** (stdlib, réutilise `zeeplib`) : `rename_slug.py <ancien-slug> [--term "…"] [--dry-run]`.
  Nouveau slug = `slugify(nouveau terme ou terme actuel)` ; `git mv` si le fichier est suivi, sinon `os.rename` ;
  met à jour `slug` (et `term`), les `related` des fiches wiki (ordre alphabétique conservé), le frontmatter `related`
  des articles de blog (remplacement ciblé sur la ligne, reste du fichier identique) et les `related` des projets DIY
  (remplacement ciblé, mise en forme préservée). Toutes les modifications sont calculées avant la première écriture :
  en cas d'erreur, rien n'est écrit. Refuse : fiche absente, slug cible existant, `--term` vide, terme déjà utilisé,
  `related` de blog non JSON sur une ligne. Sans `--term`, si le slug est déjà conforme : « Rien à faire » (code 0).
- **Convention de slug appliquée** avec le script à 5 fiches : `plaque-d-essai-breadboard`, `facture-d-electricite`,
  `production-d-electricite`, `economie-d-energie`, `fournisseur-d-electricite` (anciens slugs : même forme sans tiret
  après le « d »). 13 autres fiches liées mises à jour (plus les liens croisés entre facture et fournisseur). Les 2 liens de l'article `comprendre-facture-electricite` sont désormais
  valides (l'article n'a pas été modifié : il utilisait déjà la nouvelle forme). `court-jus-panne-de-courant` non touché.
- La docstring de `zeeplib.slugify` citait un ancien slug en exemple : reformulée (nécessaire pour le critère `git grep`).
- **Projets DIY** : champ `notionsWiki` supprimé des 3 projets (les 13 termes listés avaient tous déjà leur slug dans
  `related` : aucun ajout nécessaire) ; entrées vidéo à URL vide retirées (`"videos": []`) ; `notionsWiki` retiré de
  `src/content/config.ts`. Modification textuelle ciblée, vérifiée par comparaison JSON avant/après.
- **Gabarits** : résolution stricte des liens (erreur explicite au build), éléments provisoires masqués, compteurs dynamiques
  (détail ci-dessous).
- **README.md** : réécrit (titre corrigé, public scolaire, structure avec `AGENTS.md`, `scripts/`, `agents/`, CI, section
  Contribuer renvoyant à AGENTS.md et aux 5 scripts, chantiers ouverts actualisés ; plus de sélecteur d'âge).
- **`scripts/validate_content.py`** : aucune modification nécessaire (signale déjà `notionsWiki`, vidéos vides, slugs non
  conformes, liens blog/DIY inexistants).

## Modifications de gabarits (pour diagnostic si la CI échoue)
| Fichier | Avant → après |
|---|---|
| `src/pages/wiki/[slug].astro` (frontmatter) | `t.related.map((s) => byId[s]).filter(Boolean)` → `map` qui lève `Error("Fiche wiki introuvable « s » référencée par la fiche <slug>")` si `byId[s]` est absent. |
| `src/pages/wiki/[slug].astro` (gabarit) | `<p class="tag">N° {t.slug}{merged ? " · terme commun…" : ""}</p>` → `{t.merged && <p class="tag">Terme commun électricité/électronique</p>}` (3 fiches concernées). |
| `src/pages/wiki/[slug].astro` (gabarit) | bloc `.illustration-slot` toujours affiché (« 🎨 Illustration — à venir ») → `{t.illustration && (<div class="illustration-slot"><img …/></div>)}`. |
| `src/pages/blog/[slug].astro` (frontmatter) | `.map((s: string) => byId[s]).filter(Boolean)` → `map` qui lève une erreur citant le slug et `post.slug`. |
| `src/pages/projets-diy/[slug].astro` (frontmatter) | `.map((s) => byId[s]).filter(Boolean)` → `map` qui lève une erreur citant le slug et `d.slug` ; `videos` filtrées sur `v.url.trim().length > 0` (au lieu de `v.url`) ; nouvelle constante `hasBuyLinks`. |
| `src/pages/projets-diy/[slug].astro` (gabarit) | titre « Matériel & sourcing » + tableau 3 colonnes avec « liens à venir » → titre et tableau affichés seulement s'il y a des composants ; si aucun lien d'achat : titre « Matériel » et tableau 2 colonnes (deux expressions `cond ? (<table>…) : null`, même forme que l'original, sans `<th>`/`<td>` conditionnels) ; cellule sans lien : « — ». |
| `src/pages/projets-diy/[slug].astro` (gabarit) | « Pour aller plus loin » toujours affiché → `{cond && <h2>…</h2>}` et `{cond && <p>…</p>}` si le texte est non vide. Le bloc vidéo était déjà conditionné par `videos.length > 0` : avec `videos: []`, rien ne s'affiche. |
| `src/pages/index.astro` | « 200 notions » en dur → `{wiki.length} notions` ; « Domaines couverts » = `Object.keys(TAXONOMY).length` (24) → nombre de domaines ayant au moins une fiche, calculé via `new Set(wiki.flatMap((t) => t.data.domains))` (23 aujourd'hui, G est vide). |
| `src/lib/video.ts` | `.split("/").filter(Boolean).pop()` → `.filter((part) => part.length > 0)` (équivalent ; pour le critère grep). |
| `src/content/config.ts` | ligne `notionsWiki: z.array(z.string()).default([])` supprimée du schéma `diy`. |

Inspectés sans modification : `src/pages/wiki/index.astro`, `src/pages/blog/index.astro`, `src/pages/projets-diy/index.astro`,
`src/layouts/Base.astro`, `public/assets/site.js` (aucun texte provisoire visible ; l'attribut `placeholder` du champ de
recherche est légitime).

## Fichiers produits
- Commits sur `J1-L2` (voir `git log --oneline agents/jour-1..J1-L2`) :
  script de renommage ; renommage des 5 fiches ; nettoyage DIY + schéma ; résolution stricte des liens ;
  masquage des éléments provisoires ; compteurs de l'accueil ; README ; ce rapport.
- Aucun changeset (lot site).

## Critères d'acceptation
| Critère | Résultat |
|---|---|
| `python3 scripts/validate_content.py` : 0 erreur | ✅ 0 erreur, 3 avertissements (articles de blog courts, hors périmètre) — avant le lot : 13 erreurs |
| `grep -rn "notionsWiki\|filter(Boolean)" src` vide | ✅ vide |
| `git grep -n` des 3 anciennes formes de slug vide | ✅ vide (y compris dans ce rapport) |
| Aucune nouvelle dépendance dans `package.json` | ✅ `package.json` inchangé |
| Chaque modification de gabarit expliquée | ✅ tableau ci-dessus |

## Vérifications effectuées
- `validate_content.py` : 0 erreur(s).
- `rename_slug.py` testé sur une copie jetable du dépôt : fiche inexistante (code 1), slug cible existant (code 1),
  `--term` vide (code 1), slug déjà conforme (« Rien à faire », code 0), `--dry-run` (aucune écriture), renommage réel
  touchant wiki + blog + DIY (diff limité aux slugs, `git mv` détecté comme renommage), changement de terme seul
  (slug inchangé), dépôt sans Git (repli `os.rename`), frontmatter `related` en liste YAML multiligne (refus, aucune écriture).
- Frontmatter TypeScript des 7 pages `.astro` et `video.ts` : syntaxe vérifiée avec `node --experimental-strip-types --check`
  (Node 22). La partie gabarit (HTML/JSX Astro) n'a pas pu être compilée localement : relue deux fois, uniquement des formes
  déjà présentes dans le dépôt (`cond && (<…>)`, `cond ? (<table>…) : null`).
- Aucune résolution de lien ne lève d'erreur sur le contenu actuel : tous les `related` (wiki, blog, DIY) existent (validation).

## Points d'attention pour la relecture humaine
- **Build Astro non vérifié localement** : si la CI échoue, le tableau des gabarits indique chaque changement.
  Un message « Fiche wiki introuvable … » dans le log de build est le comportement voulu (lien cassé dans le contenu).
- **AGENTS.md §4** prévoit `reference()` d'Astro pour les références entre collections ; ce lot applique, comme demandé
  par la mission, une résolution explicite qui lève une erreur au build (schéma `related` toujours `z.array(z.string())`).
  Effet équivalent sur le build ; aligner AGENTS.md ou planifier le passage à `reference("wiki")`.
- **URLs publiques** des 5 fiches renommées modifiées (GitHub Pages ne gère pas de redirections) : les anciennes adresses
  renverront une 404. À traiter éventuellement dans le lot SEO (J4).
- **Décision prise seul (mineure)** : sur les projets DIY sans aucun lien d'achat, le titre devient « Matériel » (au lieu de
  « Matériel & sourcing ») et la colonne « Où l'acheter » est masquée ; elle réapparaît dès qu'un lien est renseigné.
- **Textes non modifiés, décision éditoriale** :
  - accueil, carte « Électricité » : promet la « mobilité électrique » alors que le domaine G n'a aucune fiche ;
  - accueil, titre : « pour apprendre en s'amusant » (ton à valider pour un public scolaire/enseignants) ;
  - pied de page (`Base.astro`) : « wiki collaboratif » alors qu'aucune contribution publique n'est possible ;
  - fiche wiki : repli « Aucun terme relié pour l'instant. » conservé (jamais affiché aujourd'hui, toutes les fiches ont des liens).
- Pour l'intégrateur : `python3 scripts/rename_slug.py court-jus-panne-de-courant --term "…" --dry-run` puis sans `--dry-run` ;
  sans `--term`, le script répond « Rien à faire » (le slug actuel correspond déjà au terme actuel).

## Hors périmètre repéré (à planifier)
- `src/pages/wiki/index.astro` : les 24 boutons de filtre incluent le domaine G vide (clic = liste vide) ; à traiter avec
  la reclassification des domaines (J3).
- `src/content/config.ts` ne déclare pas `niveau`, `versionSimple`, `sources`, `relecture` (acceptés par `validate_content.py`) :
  Zod les retirerait silencieusement des données Astro ; à ajouter dans le lot schéma (J2-L1) pour qu'ils soient affichables.
- `validate_content.py --stats` compte les fiches « Relues » avec `statut == "relu"`, alors qu'AGENTS.md utilise `relu-ia`
  et `valide` : le compteur restera à 0.
- `LISEZ-MOI.txt` à la racine : consignes d'un ancien zip (remplacement du logo), obsolète ; suppression à décider.
- Les 3 articles de blog sont courts (142 à 213 mots, cible ≥ 800) : lot de réécriture prévu (J4).
