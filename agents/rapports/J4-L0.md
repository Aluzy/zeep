---
lot: J4-L0
titre: "Cliquet de dette éditoriale, mapping des niveaux incrémental, modèle de rapport structuré"
date: 2026-09-25
statut: termine
redacteur: agent-J4-L0
controleur: aucun
changesets: []
fiches_creees: []
fiches_modifiees: []
lacunes: []
decisions_humaines: ["Type de source « officiel » (86 fiches) : l'accepter (l'ajouter à TYPES_SOURCE dans scripts/dette.py et à AGENTS.md) ou le convertir en « reference » / « programme » ?", "Relecture jugée indépendante quand « par » contient « controleur » : convention à confirmer.", "Détection « secteur 230 V » par mots-clés (src/data/dette.json) : trier les 24 fiches signalées, exempter les faux positifs avec leur raison."]
---

# Rapport de lot J4-L0 — Cliquet de dette éditoriale

Rapport écrit a posteriori (le 25/09/2026, lot J4-CHAINE) à partir du commit `be66b91` et de la
note de passation : le lot avait été poussé à la main, sans rapport.

## Ce qui a été fait
- `scripts/dette.py` (nouveau) : les 9 règles d'AGENTS.md §3 vérifiées par script ; options `--detail REGLE`,
  `--indicateurs`, `--abaisser`, `--initialiser`.
- `src/data/dette.json` (nouveau) : base du cliquet (fiches déjà en défaut, tolérées) et configuration de la
  détection « secteur 230 V » (mots-clés, formules de rappel, exemptions motivées : `joule`, `kilowatt`,
  `regime-transitoire`).
- `scripts/validate_content.py` : appelle `dette.verifier()` ; un défaut sur une fiche hors base est bloquant.
- `agents/outils/mapping_niveau.py` : rendu incrémental (`--lot`, entrées de TABLE périmées, jamais d'écrasement).
- `agents/rapports/TEMPLATE.md` : en-tête structuré, indicateurs avant / après, section du contrôleur.
- `AGENTS.md` (§2.3, §2.6, §3, nouveau §3 ter), `README.md`, workflow CI.

**Incident** : cinq fichiers ont été poussés hors de leur dossier (`dette.py`, `dette.json`, `deploy.yml` à la
racine, `validate_content.py` dans `agents/outils/`, le CSV J2-L2 dans `agents/donnees/`). La CI tournait donc
sur les anciennes versions et le cliquet n'était pas actif. Corrigé par le commit
« J4-L0: replace les fichiers de l'outillage à leur emplacement » (lot J4-CHAINE), cliquet vérifié : une
définition raccourcie sur une fiche hors base est bien bloquée.

## Indicateurs (état de départ du cliquet)

| Indicateur | Valeur |
|---|---|
| Définitions de 25 à 60 mots | 204/387 (53 %) |
| Version simple, notions vues avant le lycée | 7/28 (25 %) |
| Avec au moins une source précise (URL non racine) | 27/387 (7 %) |
| Relues par un contrôleur indépendant | 0/387 |
| Validées par Alexandre | 0/387 |
| Fiches secteur sans rappel de sécurité | 24 |

## Vérifications effectuées
- `python3 scripts/validate_content.py` : 0 erreur (après remise en place des fichiers).
- `python3 scripts/dette.py` : code de sortie 0, aucune dette nouvelle.
- `python3 agents/outils/mapping_niveau.py --verifier` : OK.
- `python3 scripts/audit_couverture.py --ci` : code de sortie 0.

## Relecture (rempli par le contrôleur)
Sans objet : lot d'outillage.

## Points d'attention pour la relecture humaine
- Les trois choix listés dans `decisions_humaines` ont été faits sans validation d'Alexandre.
